{
  "name": "referral-credit-network-subgraph",
  "version": "1.0.0",
  "description": "The Graph subgraph for ReferralCreditNetwork smart contract",
  "main": "index.js",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/",
    "create-local": "graph create --node http://localhost:8020/ referral-credit-network",
    "remove-local": "graph remove --node http://localhost:8020/ referral-credit-network",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 referral-credit-network"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.56.0",
    "@graphprotocol/graph-ts": "0.30.0"
  },
  "devDependencies": {
    "typescript": "^4.9.0"
  }
}
